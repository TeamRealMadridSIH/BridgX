services:
  - type: web
    runtime: static
    name: bridgex
    rootDir: Software/Frontend
    buildCommand: npm ci && npm run build
    staticPublishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
